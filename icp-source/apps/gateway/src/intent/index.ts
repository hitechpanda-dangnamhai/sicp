/**
 * apps/gateway/src/intent/index.ts
 *
 * S-02 T07 emit. Barrel for intent module — primarily `IntentModule` for
 * `app.module.ts` import. IntentService + IntentController are internal.
 */

export { IntentModule } from './intent.module';
