/**
 * `@icp/shared-types/sse` — SSE subpath barrel.
 *
 * S-02 T07 emit. Mirrors `behavior/` pattern (C-32 dual access LOCKED).
 *
 * @see ./intent-stream.ts for full schema catalog.
 */

export * from './intent-stream';
