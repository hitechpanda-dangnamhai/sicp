# S-P0-01 — Multi-tenant SaaS (RLS + tenant_id)

## Goal
Mọi data path scope theo tenant, enforcement 3 lớp: RLS (DB) + app-level
helper + Vespa/Redis scope. Đóng BACKLOG §3 P0 #1; unblock #2 payment.

## Design (chỉ trỏ, không lặp body)
ADR-040 (+ amendment 2026-06-11: icp_app/SET LOCAL, 2 conn string, Vespa
helper, Redis prefix; + amendment b: NULLIF hardening) · ADR-046
(+ amendment: active-tenant JWT) · ADR-047 (header transport — code align
theo doc; + amendment 2026-06-12: header contract áp MỌI caller của MCP,
gồm Gateway direct tool call; MCP fail-closed sau T03) · ADR-041 (không
log PII).

## Evidence (RECON 2026-06-11)
- `icp` role superuser+BYPASSRLS; pg pool max=10 (gateway), psycopg
  conn-per-query (MCP); không pooler.
- 0 tenant_id / 17 bảng (FACTS); FK→users 8 bảng; products.merchant_id có sẵn.
- JWT thiếu tenant claim; Gateway→AI dùng JSON body user_id (lệch ADR-047).
- UNIQUE global vỡ multi-tenant: orders.idempotency_key, policies.code.
- behavior_events partitioned PK(event_id, occurred_at) — thêm cột, không đổi PK.
- 3 matview V006 GROUP BY thiếu tenant; trigger trg_review_update_product.
- Data thật: orders 1281, products 75, users 5 → backfill tenant `demo`.
- Vespa product.sd có merchant_id, YQL builder (vespa.py:147) không filter.
- Redis: idem:cache scope user; sse:pubsub + RedisSaver thread_id chưa scope.

## Evidence bổ sung (RECON 2026-06-12 — STOP T03, đã verify lại bằng grep)
- Gateway có **2 đường** vào MCP `/rpc`, cả 2 đều KHÔNG gửi
  `X-Tenant-Id`/`X-User-Id` (user_id nằm trong params):
  (1) `clients/mcp.client.ts` — dùng bởi cards.controller (×3 call),
  products.controller (×2: products.update + vespa.index),
  dashboard.service (×1), **intent-suggest-attrs.controller** (×1:
  vision.suggest_attributes — report STOP cũ thiếu chỗ này);
  (2) `cart/cart.service.ts` — client JSON-RPC **tự chế raw fetch**
  (dòng 114, di sản S-05 trước khi McpClient ra đời S-07), 7 tool cart.*.
  Report STOP cũ nói cart dùng mcp.client → SAI.
- Verified: KHÔNG có call-site MCP nào ngoài HTTP request flow
  (không onModuleInit/@Cron/seed); setInterval/setTimeout hiện hữu chỉ là
  abort-timeout + SSE heartbeat. KHÔNG có đường raw-fetch /rpc thứ 3.
- Evidence (sửa lần 2, 2026-06-12, kèm quote chống vòng lặp):
  server.py:81 KHÔNG phải tenant-context stub — là W3C traceparent carrier
  (AC-11 trace continuity). Quote nguyên văn server.py:78-82:
  ```
          # --- W3C traceparent extract (server-side, AC-11) ---
          # propagator.extract reads "traceparent" + "tracestate" headers from
          # carrier dict; returns Context with remote SpanContext attached.
          carrier = {k.lower(): v for k, v in request.headers.items()}
          ctx = propagator.extract(carrier=carrier)
  ```
  grep apps/mcp/src: KHÔNG có X-Tenant-Id / current_tenant / contextvar tenant.
  tenant_connection(tenant_id) (db.py:39) nhận đối số tường minh, không đọc context.
  → Plumbing header→contextvar phía MCP = XÂY MỚI, thuộc T03b.
- cart Redis key `cart:{user}`: CHỈ MCP chạm (Gateway đi qua cart.* tool,
  cart.controller.ts:31) → cart dev hiện có thành orphan khi đổi key,
  chấp nhận (comment WHY tại chỗ, không ADR).
- DATABASE_URL runtime đã là icp_app từ T02 (.env.example:21) → RLS
  enforce thật; idem:cache thuần Gateway → ngoài scope T03 (P0 #31).

## Tasks (lát dọc, test NHÚNG)

### T01 — Contract-as-code
V011 migration: `tenants` + `tenant_memberships` + `tenant_id NOT NULL` mọi
bảng tenant-scoped (TRỪ users/sessions — ADR-046) + backfill tenant `demo`
(comment WHY đầu file) + composite UNIQUE `(tenant_id, idempotency_key)` /
`(tenant_id, code)` + behavior_events cột+index mọi partition + role
`icp_app` NOBYPASSRLS + RLS policy mọi bảng + recreate 3 matview +tenant_id.
Env: `DATABASE_URL_MIGRATE` tách khỏi `DATABASE_URL` (Zod fail-fast).
Types: Zod Tenant/Membership/JWT claim → `pnpm openapi:sync`.
Stub chạy được: Gateway `withTenant()` + MCP context manager + MCP Vespa
helper (inject filter) — compile + boot OK. Commit kèm 2 amend note ADR.
Test nhúng: migrate idempotent; connect icp_app SELECT cross-tenant = 0 row.

### T02 — Gateway vertical
Login + refresh lookup memberships → JWT claim `tenant_ids[]` (không default
active); `POST /auth/switch-tenant` UPDATE sessions.last_active_tenant_id +
return redirect_url (KHÔNG re-issue token — ADR-046 amend c);
GET /auth/landing redirect theo last_active hoặc /onboarding;
TenantMembershipGuard check URL.tenant_id IN jwt.tenant_ids;
mọi repository qua `withTenant()`; forward `X-User-Id`/`X-Tenant-Id` sang AI
(GIỮ body.user_id song song; xoá ở T03 — 2-phase CLAUDE.md §11, ADR-047);
idem key prefix tenant; `TenantResolverService` chain JWT→X-Tenant-Id→400
cho anonymous endpoint (ADR-046 amend b); fix auth.service.spec.ts fixture.
V013 NULLIF hardening mọi RLS policy + WITH CHECK + behavior_events
partitions — empty GUC → NULL → 0 rows silently (ADR-040 amend b).
Test: 2 tenant seed, REST cross-tenant 404/0; anonymous /track với
X-Tenant-Id persist đúng tenant; isolation strict 0-rows khi GUC
unset/empty (không throws).

### T02b-1 — FE routing foundation + shim + /home
(Sau T02 — cần sessions.last_active_tenant_id + TenantResolverService.)
`app/s/[slug]/layout.tsx` + slug resolve/validate vs jwt.tenant_ids (∉ →
404); helper `tenantHref()` — CẤM hardcode `/s/` ngoài helper (grep gate
T02b-3); middleware.ts: port auth-gate (dòng 68-80) + dual-shim (ADR-046
amend d): forward-rewrite `/s/<slug>/X`→`/X` cho nhóm chưa migrate +
legacy redirect 308 `/X`→`/s/<last_active_slug>/X` cho nhóm đã migrate;
9 shared component (DashboardHeader, SearchHeader, BottomNav, 4
organisms, 2 page) link qua helper — link `/me` + `/auth` GIỮ global;
migrate /home + 27 ref. Test: `/s/<slug>/intent-01` (chưa migrate) render
qua rewrite; `/home` redirect đúng slug; slug sai tenant → 404; 2 tenant
không lẫn.

### T02b-2 — Intent group migration
mv 7 trang `/intent-01..07` → `app/s/[slug]/intent/0X` + ~15 ref + xoá
entry forward-rewrite `/intent`. Verify `/dev/acceptance/intent-0X`:
nếu hardlink route thật → qua helper với tenant demo (dev giữ global).
Đo diff rename detection (`git diff -M`); vẫn >1500 → cắt đôi 01–04 /
05–07. Test: deep link `/s/<slug>/intent/0X` 2 tenant; intent flow e2e
không lẫn.

### T02b-3 — Cleanup + gate
/me/* GIỮ global (ADR-046 — account-scope); verify /me/notifications:
nếu fetch tenant data → resolver chain JWT/X-Tenant-Id, KHÔNG move trang.
Xoá toàn bộ forward-rewrite (legacy redirect GIỮ vĩnh viễn — bookmark);
grep gate: 0 hardcoded `/home|/intent-` ngoài helper + middleware
redirect map. Test: e2e navigation toàn app 2 tenant; mọi URL cũ
redirect đúng; /me hoạt động cả khi switch tenant.

### T02c — Gateway→MCP tenant propagation (chèn từ STOP T03 2026-06-12)
(Gateway-surface ONLY. Trước T03 — MCP enforcement chưa bật, header thừa
vô hại = cửa sổ trung gian an toàn. Call-site đã PLAN-verify 06-12, xem
Evidence — không cần grep lại trừ khi code đổi.)
Helper chung `buildMcpIdentityHeaders(ctx)` (1 nơi duy nhất build
`X-Tenant-Id` + `X-User-Id` từ request context — TenantResolverService/
`withTenant()` có từ T02) → áp vào CẢ 2 đường:
(1) `clients/mcp.client.ts` `call()` — phủ cards/products/suggest-attrs/
dashboard; (2) `cart/cart.service.ts` raw-fetch client — phủ 7 tool
cart.*. KHÔNG refactor cart.service sang McpClient trong task này
(diff risk; ghi consolidation vào BACKLOG P1 khi sync). GIỮ params
user_id song song — xoá ở T03 (2-phase, cùng pattern body.user_id
Gateway→AI của T02; ADR-047 amend 06-12).
Test nhúng: unit assert 2 header trên outbound của CẢ 2 client; e2e
cart/cards/dashboard/suggest-attrs 2 tenant vẫn xanh (MCP chưa enforce —
regression guard, không phải isolation test).

### T03 cắt 3 task (2026-06-12, PLAN duyệt) — thực thi T03a → T03c → T03b
T03 nguyên bản vượt trần ~1500 dòng (RECON: 20 connect-site MCP + 14 init/
38 call site McpClient qua 6 graph + re-key + 2-phase). Cắt theo nhịp
propagate-trước-enforce-sau (mirror T02c→T03, ADR-047 amend c).

### T03a — AI-side additive: header + dual-publish + re-key AI-internal
PREREQ: T02c merged.
SCOPE:
1. AI đọc `X-Tenant-Id`/`X-User-Id` (Gateway→AI) → `state.tenant_id`/
   `state.user_id` cho 6 graph. Contextvar KHÔNG tự propagate qua
   threading.Thread → truyền TƯỜNG MINH qua state/tham số phía AI, không
   dựa contextvar.
2. `mcp_client._build_headers` (mcp_client.py:74) inject `X-Tenant-Id` +
   `X-User-Id`; luồng tenant qua 14 init site + 38 call site.
3. Dual-publish SSE (ADR-048 amend c): redis_publisher.py:57 publish CẢ
   `sse:pubsub:{rid}` cũ VÀ `sse:pubsub:{tenant}:{rid}` mới. Dev-subscribe
   main.py:106/256 chuyển kênh mới.
4. Re-key AI-internal (ADR-040 iv): `voice:context:{tenant}:{user}` (2
   file); RedisSaver thread_id = `{tenant}:{rid}` (main.py:379). Checkpoint
   cũ TTL 30min tự hết — không migrate.
5. GIỮ user_id trong params song song (2-phase). MCP CHƯA enforce.
Test nhúng: header presence Gateway→AI→MCP; dual-publish đủ 2 kênh; key mới
voice:context/thread_id. Merge an toàn: không đổi contract nào Gateway đang đọc.
STOP: diff > ~1500 → báo PLAN.

### T03c — rid ownership + Gateway rid-key tenant-scope (F1+F2+F3)
PREREQ: T02 (TenantResolverService). Độc lập MCP enforcement. Chạy sau T03a.
SCOPE:
1. F1 (khôi phục ADR-019 — KHÔNG amend ADR): `@Get('stream')` thêm
   `JwtAuthGuard` CHỈ (cookie httpOnly). KHÔNG `TenantMembershipGuard` —
   /stream mở bằng native EventSource, không set được `X-Tenant-Id` (đúng
   constraint đã ghi trong Context ADR-019); resolver header-only sẽ luôn
   400. Isolation tenant đạt qua ownership-check mục 2.
2. Ownership binding: POST /intent lưu owner `{user_id, tenant_id}` vào
   VALUE intent:cache lúc tạo rid; /stream và POST /:rid/action verify
   `value.user_id === jwt.user_id` AND `value.tenant_id ∈ jwt.tenant_ids`
   TRƯỚC subscribe/resume. Mismatch → 404 đồng nhất cache-miss.
3. F3: intent:cache GIỮ key `{rid}`, owner `{user_id, tenant_id}` nằm trong
   VALUE (POST /intent lưu lúc tạo rid — tenant resolve được vì POST đi qua
   fetch có header). Cô lập cross-tenant qua ownership-check value, không
   qua key-prefix. `intent:action:{tenant}:{rid}:{attempt_n}` GIỮ re-key
   như cũ (đi fetch, có header).
4. Orphan window chấp nhận: rid in-flight lúc deploy đổi key intent:cache
   mồ côi, TTL 60s tự chết — comment WHY tại chỗ (tiền lệ cart), không migrate.
NGOÀI SCOPE (chủ ý, KHÔNG tự kéo vào): BACKLOG #31 (Idempotency MW chạy
trước guard, tenantScope client tự khai — idem:cache thuần Gateway, slice
đã để ngoài T03, đã có chỗ ở queue P0 #31).
Test nhúng: user tenant X cầm rid tenant Y → /stream 404 + /:rid/action 404;
cookie invalid → 401; happy-path SSE e2e không vỡ.
STOP: diff > ~1500 → báo PLAN.

### T03b — MCP enforcement + đóng 2-phase + Gateway switch kênh
PREREQ: T03a + T03c merged. (#32 đã đóng — BACKLOG §3 #32 ✅ FIXED 06-12,
commit 8dab0ff: Dockerfile rm dist + tsc thay nest build.)
SCOPE:
1. MCP: set contextvar tenant/user trong `rpc()` (server.py, sau traceparent
   extract) từ header. KHÔNG đổi chữ ký `dispatch(method, params)`
   (tools/__init__.py:87) — evidence RECON [2]. KHÔNG tự mở rộng.
2. 20 connect-site → `tenant_connection(current_tenant())`.
3. Fail-closed: RPC thiếu `X-Tenant-Id` → error. Tool global (nếu có, vd
   auth.verify_jwt) phải whitelist TƯỜNG MINH trong code + test.
4. Cart: identity từ header thay params; re-key `cart:{tenant}:{user}`
   (cart.py:70-71,124-129).
5. Đóng 2-phase: gỡ user_id khỏi 10 param dict AI + Gateway dead-param.
6. SSE switch: Gateway build kênh `{tenant}:{rid}` với tenant =
   `value.tenant_id` của intent:cache SAU KHI verify ownership (T03c) —
   KHÔNG lấy "tenant từ JWT" (JWT chỉ có `tenant_ids[]`, không có active
   tenant — ADR-046). Giá trị này khớp tenant T03a dùng dual-publish (cùng
   resolve lúc POST /intent). AI gỡ publish kênh cũ ở redis_publisher.py:57.
7. Chuyển gate wait-subscriber main.py:256 sang kênh {tenant}:{rid}
   (đồng thời với Gateway switch — nợ T03a, deviation đã PLAN ratify).
8. Dọn dead constant _SSE_PUBSUB_CHANNEL_TEMPLATE main.py:106 (nợ T03a).
Test nhúng: tenant isolation per tool; thiếu header fail-closed; cart
cross-tenant cách ly; anti-spoof Gateway+AI header; SSE e2e kênh mới.
STOP: diff > ~1500 → báo PLAN cắt tiếp.

### T04 — Vespa + analytics vertical
product.sd +field tenant_id; backfill doc hiện hữu bằng PARTIAL-UPDATE
(CẤM re-feed — embeddings Vespa-only, ADR-036); 6 tool vespa.* ép qua
helper; vespa.index ghi tenant_id; matview refresh verify + dashboard
queries scope. Test: hybrid_search cross-tenant 0 row; matview đúng theo
tenant. (Đồng thời đóng BACKLOG P0 #9.)

### T05 — E2E + đóng slice
Seed tenant thứ 2 thật; e2e Playwright: search/cart/order/cards isolation;
quét residual (grep query thiếu helper + grep params user_id còn sót);
`bash scripts/gen-facts.sh` → FACTS `cột tenant_id > 0`; sync BACKLOG
§2/§3/§4.

## Stop conditions
- Phát hiện pooler transaction-mode trên đường runtime → STOP (SET LOCAL
  semantics vỡ), quay lại PLAN.
- Bất kỳ query/tool không thể đi qua helper → STOP, không bypass tay.
- Code path nào đòi tenant_id trên users/sessions → STOP (vi phạm ADR-046).
- Vespa backfill đòi re-feed mất embedding → STOP (ADR-036 Locked).
- Call-site MCP nào nằm NGOÀI HTTP request context (cron/seed/worker) —
  không có tenant để gửi header → STOP, báo PLAN xếp đường riêng.
- Task nào diff > ~1500 dòng → cắt đôi, báo lại PLAN.
