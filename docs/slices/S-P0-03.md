# S-P0-03 — Safety-net (Cluster C2, ADR-052)

> Slice 2 của chương trình re-architecture. Mục đích: dựng LƯỚI (CI thật + test
> + eval + cost-trace) TRƯỚC khi bất kỳ refactor lớn nào (C3/C7) được phép chạy.
> Nhận 17 ID theo map BACKLOG §3: A13 · W-32/37/40/46/54/55/56/74/75/76/77/78/
> 79/80/81/93 (+2 nợ C1 đẩy sang: seed customer password→W-75 · deploy-drift
> gate→W-76). Quyết định kiến trúc: trỏ ADR-052 (program) · ADR-054 (cost/trace).

## §-1 PRECONDITIONS (Code kiểm TRƯỚC nghi thức MỞ TASK — fail dòng nào DỪNG báo)

```
ls docs/INVENTORY_2026-06-13.md docs/slices/S-P0-03.md .github/workflows/ci.yml \
   .github/workflows/guards.yml infra/migrations/V014__housekeeper_safety.sql
git log --oneline -1   # phải là commit close S-P0-02 hoặc sau nó
grep -c "S-P0-02" docs/MASTER_BACKLOG.md   # ≥2 (registry + done)
```

## §0 Luật ràng buộc toàn slice

1. Re-verify (CLAUDE.md DoD): finding slice nhận = giả thuyết — verify tại
   path:line trước khi code, CONFIRMED/STALE vào report.
2. Tenant gate (CLAUDE.md DoD): data-path mới (bảng `llm_traces` T03) phải có
   tenant_id + assert cross-tenant.
3. Diff/task ≤ ~1500 dòng (lock/generated/golden-fixture không tính). Vượt → cắt.
4. Test NHÚNG trong task. Đóng slice = nghi thức CLAUDE.md §8 + OUTPUT R4 + Pha D.
5. STOP §4. Slice-specific: gate CI mới làm ĐỎ pipeline trên main hiện tại →
   DỪNG báo (không silent-skip, không hạ gate để xanh giả).
6. Thứ tự task CỨNG: T01 (CI thật) đi đầu — mọi task sau hưởng gate; T03 trước
   T04 (eval cần trace schema).

## §1 — T01: CI hard-fail + deploy-drift gate + contract gate

Nhận: W-76 · W-46 · A13(phần gate) · nợ deploy-drift C1.
1. `.github/workflows/ci.yml`: gỡ MỌI soft-fail (`|| echo`, `continue-on-error`)
   — lint/test/typecheck FAIL = pipeline FAIL. Lint phủ workers (nợ KI#5 T02).
2. Coverage gate kiểu RATCHET: đo coverage hiện tại từng app (gateway/web/
   workers/ai/mcp) → set floor = số đo được (làm tròn xuống 1%) → CI fail nếu
   tụt dưới floor. KHÔNG đặt 70% ảo ngay — floor nâng dần mỗi cluster. Ghi
   floor vào file config commit được (`coverage.floors.json`).
3. W-46: gate `pnpm openapi:sync` drift — chạy sync trong CI, `git diff
   --exit-code` trên `packages/shared-types/` → drift = FAIL.
4. Deploy-drift gate (bài học T06 + BACKLOG #32): build-arg `GIT_SHA` stamp
   vào image (gateway/ai/mcp/workers/web) expose qua endpoint health/label;
   script `scripts/smoke-live.sh`: so SHA container vs HEAD + smoke login/
   intent/cart/health → dùng được cả tay lẫn CI-stage deploy (CI chỉ chạy khi
   có môi trường compose — nếu runner không có, stage này điều kiện hoá, ghi rõ).
Acceptance: PR thử có lint-error → CI đỏ · coverage tụt giả lập → đỏ · sửa DTO
không sync → đỏ · `smoke-live.sh` bắt được container cũ (test bằng cách không
rebuild 1 service) · main hiện tại XANH với floor đo thật.

## §1b — T01b: CI tier-2 (Postgres + Redis) un-skip integration specs (tách từ T01 KI#3)

Nhận: phần DB-integration của W-76 (un-skip + floor with-DB). Job `integration-db`
trong `ci.yml`: postgres:16-alpine + redis:7-alpine + `apply.sh` migrations + seed
tối thiểu (`infra/seed/ci-min.sql` — CHỈ merchant1, KHÔNG full W-80) → un-skip 19
spec (`RUN_DB_TESTS=1`: gateway auth 12 + tenant-isolation 7) + mcp stock-atomic +
re-ratchet gateway floor 29→43 (đo with-DB). Gate-bite: postgres down + RUN_DB_TESTS=1
→ suite fail (KHÔNG skip) → job đỏ.

**Known classes (bài học, áp cho mọi spec sau):**
- **test-vs-RLS:** Mọi integration spec ĐỌC/XOÁ bảng tenant-scoped sau S-P0-01 phải
  dùng `withTenant(tenant_id, cb)` HOẶC super-pool có chủ ý. Pool `icp_app` không set
  tenant ctx → RLS lọc 0 dòng (read) / xoá 0 dòng (cleanup → row tích luỹ) = **test
  stale, KHÔNG phải code bug**. Bài học: `auth.service.spec` AC-12.a/b/c (event ghi
  ĐÚNG dưới DEMO — log `tracker.batch_persisted accepted:1` — nhưng read + afterEach
  DELETE qua `pool` icp_app no-ctx → 0). Fix: read/delete qua `pgPool.withTenant(DEMO)`.

## §1c — T01c-hotfix: `_fetch_price_solver` identity NameError (phát hiện T01)

Nhận: nợ hotfix từ T01 (ruff F821 noqa-baseline). Tham chiếu `BACKLOG §3` dòng "T01c-hotfix".

[VIỆC] Fix bug runtime trong `apps/ai/src/graphs/intents/importing_by_images.py`: hàm
`_fetch_price_solver(mcp_client, context)` gọi `identity_kwargs(state)` với `state` UNDEFINED
trên nhánh `avg>0` → NameError KHÔNG bắt bởi `except McpError` → identity (X-User-Id/X-Tenant-Id)
KHÔNG thread vào `analytics.suggest_price`.

[RE-VERIFY] (1) Đọc `importing_by_images.py` path:line định nghĩa `_fetch_price_solver` —
confirm signature thiếu `state`; (2) trace caller(s) — xác định `state` thực sự cần truyền hay
`context` đã đủ; (3) confirm `analytics.suggest_price` ở MCP layer đọc identity từ header (T02c
pattern), không từ params.

[VIỆC chi tiết]
- a. Sửa signature/closure để `identity_kwargs` có nguồn `state` hợp lệ (truyền `state` vào hàm
  HOẶC dùng `context.state` nếu pattern graph cho phép HOẶC loại bỏ `identity_kwargs` nếu MCP
  layer đã đọc identity từ header — chọn phương án đụng diff ít nhất, code đúng nhất).
- b. Gỡ noqa baseline T01 trên dòng tương ứng (luật ratchet: nợ giảm dần).
- c. Test nhúng: unit test trên `_fetch_price_solver` hoặc graph-level test đảm bảo nhánh `avg>0`
  không throw + identity được forward đúng (mock `mcp_client.call` assert kwargs có
  X-User-Id/X-Tenant-Id hoặc params phù hợp).

[RÀNG BUỘC] DoD §5 đầy đủ — code path AI tenant-scoped → tenant-gate ÁP DỤNG (assert identity
thread đúng). Stop §4. Diff nhỏ (~vài chục dòng).

[ACCEPTANCE] (1) Re-verify 3/3 CONFIRMED; (2) test nhúng pass; (3) toàn bộ suite ai/mcp pass;
(4) noqa baseline tương ứng được gỡ; (5) không đụng code ngoài hàm `_fetch_price_solver` +
caller trực tiếp; (6) `BACKLOG §3` dòng T01c-hotfix → tick xong.

## §2 — T02: Test foundation — e2e + seed đa-tenant (SPLIT 3, reconcile 2026-06-13)

> Re-verify T02 @HEAD 50af0d9 → 3 premise STALE/CHANGED, đã reconcile:
> - "2 web test fail" GỠ (vitest 264/264 green @50af0d9).
> - "8 intent" → đếm intent RUNNABLE thật từ router (pin tại T02b, KHÔNG hardcode 8);
>   I06 payment=placeholder (C4 chưa build); I08=auth platform-flow → map e2e auth-flow đã có.
> - W-75 gốc = `ci-min.sql` chỉ seed merchant1 (KHÔNG bug parse — seed.ts:124 hash đủ mọi user).
>   Đóng W-75 = seed customer + dẫn coverage cross-user STRUCTURAL sẵn có
>   (idempotency.interceptor.spec.ts:56 + intent.controller.spec.ts:38, green) — KHÔNG xây
>   e2e cross-user mới. Tenant-gate ADR-052 vẫn bắt: xây e2e cross-TENANT.
> Nhận (giữ): W-75 · W-80 · W-81 · A13(test). Thứ tự CỨNG: T02a → T02b → T02c.

### §2a — T02a: Seed foundation đa-tenant + ci-min mở rộng
Nhận: W-80 + phần seed của W-75.
1. W-80: seed ≥2 tenant đầy đủ (products/policies/memberships) + ≥1 customer global, đủ
   data cho mọi intent live chạy. Nhà: `infra/seed/`.
2. Mở rộng `infra/seed/ci-min.sql`: thêm customer (+ tenant#2 tối thiểu) — GIỮ merchant1
   FIXED id (test_t05_stock_atomic.py:30 hardcode) BẤT BIẾN.
3. Seed path tôn trọng tenant_id; qua app-pool → withTenant / super-pool tường minh.
Acceptance: seed CI → ≥2 tenant + customer hiện diện (assert count) · merchant1 id KHÔNG
đổi · integration-db T01b re-run green.

### §2b — T02b: e2e flows (SPLIT-2 theo RISK, pre-flight 2026-06-13 @a1b6240)

> Pre-flight: 6 intent runnable PIN = 01,02,03,04,05,07 (main.py:342-414); I06=placeholder
> (no graph) · I08→auth-flow (no graph/page, KHÔNG nhân bản). Baseline e2e RED 2 nguyên nhân
> KHÁC nhau: (i) `auth-flow` pw = stale-SELECTOR (`getByLabel('Mật khẩu')` match input+eye-toggle
> → strict; fix `{exact:true}`); (ii) `dashboard.spec` + `auth-flow` AC-38.1 = stale-FLOW —
> S-P0-01 multi-tenant GỠ /home dashboard → nay `/s/[slug]/home` qua onboarding picker "Chọn shop".
> KHÔNG app-bug (app cố ý storefront-scope). KHÔNG deploy-drift. Cắt RISK: deterministic (CI-gate được) vs
> LLM-driven (flaky-isolated). Thứ tự: T02b-1 → T02b-2.
> **Note carry merchant2 dual-tenant:** seed merchant2 thuộc CẢ demo (V011 backfill) LẪN demo2
> (T02a owner) — product tenant_id đúng nên isolation product-level KHÔNG vỡ, nhưng merchant-side
> view cross. ⇒ isolation assert lấy chủ thể STOREFRONT (customer-facing) demo vs demo2, KHÔNG
> dùng merchant2 (hợp lệ nhiều tenant → che leak thật).

#### §2b-1 — T02b-1: deterministic e2e + CI-gate-able
Nhận: W-75(e2e deterministic) + tenant-gate ADR-052.
1. Sửa baseline 2 spec (spec-only, KHÔNG đụng app UI):
   - `auth-flow` Mật khẩu: selector `{exact:true}` (stale-selector, trivial).
   - `dashboard.spec` + `auth-flow` AC-38.1: REWRITE nav-flow theo multi-tenant thật:
     login → /home(redirect) → /onboarding "Chọn shop" → pick "Demo Shop" → `/s/demo/home`
     (dashboard/DashboardHeader). Đây là align-test, KHÔNG đổi app behavior.
2. Auth e2e: login/logout/refresh — gộp/bù `auth-flow.spec.ts` hiện có.
3. Cart 7-route e2e.
4. Cross-TENANT storefront isolation e2e (BẮT BUỘC, tenant-gate): storefront tenant demo KHÔNG
   thấy data demo2 (≥1 assert đọc, +ghi nếu có). Chủ thể STOREFRONT (Note carry — KHÔNG merchant2).
Acceptance: baseline+auth+cart+isolation xanh local compose · isolation đỏ khi giả lập leak
(demo+revert) · 0 LLM live call (deterministic thuần) · RLS-aware (withTenant/super-pool tường minh).

#### §2b-2 — T02b-2: LLM-driven intent e2e (flaky-isolated)
Nhận: W-75(e2e intent).
1. 6 intent happy-path (01-05,07) SSE qua EventSource — assert STRUCTURAL (reach result-state/
   SSE-complete, KHÔNG assert content LLM). I06=placeholder page assert.
2. Fixtures image/voice base64 (golden — ngoài cap line-count).
3. CI posture = ADR-056: live-LLM, assert STRUCTURAL, job NON-BLOCKING (report, KHÔNG fail
   pipeline). Keys CI secret. Wiring thực thi ở T02c.
Acceptance: 6 intent reach result-state ổn định local · fixtures committed · CI-posture quyết+ghi.

> Note Vespa (#36→S-P0-01): search(03)+recommend(04) reach-result CHẶN bởi Vespa feed tenant_id gap
> (homed S-P0-01). T02b-2 khi mở: làm intent KHÔNG đụng Vespa (01/02/05/07) trước HOẶC chờ Vespa fix.

> Wire CI (§2c): T02b-1=hard-gate · T02b-2=non-blocking live job (ADR-056).

### §2b-hotfix — T02b-hotfix: FE intent POST thiếu X-Tenant-Id (regression S-P0-01, phát hiện T02b-2 spike)
Nhận: regression cross-cutting (mọi intent web → 400 TENANT_CONTEXT_MISSING). Ref ADR-046 amend c
(resolver HEADER-ONLY LOCKED — CẤM sửa) · ADR-056 (spike bắt được).
[RE-VERIFY] (1) 6/6 stream-hook intent thiếu X-Tenant-Id tại path:line; (2) `tenantHeaders()`
(api-client.ts:23, nguồn getActiveTenantId) trả đúng khi active-tenant set qua picker; (3) KHÔNG
path nào khác inject (storefront+authed đều vỡ) → CONFIRMED/STALE report.
[VIỆC] 6 stream-hook intent (use-search-stream + import/recommend/cart/voice-buy/voice-analyze)
attach `...tenantHeaders()` vào headers fetch POST /api/v1/intent (pattern tracker.ts:181). CHỈ FE
hook — KHÔNG đụng resolver (LOCKED), KHÔNG đụng BE.
[TEST nhúng] (a) unit/vitest: mock getActiveTenantId → assert hook fetch headers chứa X-Tenant-Id
(logic); (b) e2e intent-03 (promote spike spec → regression test thật) reach result-state LIVE sau fix.
[ACCEPTANCE — ratified 2026-06-13, deviation]: scope = DIỆT TENANT_CONTEXT_MISSING. 6/6 hook
attach header ✓ · resolver UNCHANGED ✓ · re-verify 3/3 ✓ · unit 2/2 ✓ · e2e intent-03 POST 202
(pre-fix 400) = regression guard tầng tenant. reach-result-state DEFER → blocked bởi Vespa
hybrid_search 400 (BACKLOG §3 #36, finding riêng, chặn T02b-2 search-reach); intent-03 mở rộng
assert result-state khi Vespa fixed.

### §2c — T02c: contract-Zod assert + wire CI
Nhận: W-81 + A13(CI).
1. W-81: response route chính parse qua Zod từ `packages/shared-types` trong e2e (contract
   thật, không chỉ shape).
2. Wire e2e vào CI (gate T01) — fail=pipeline fail. Điều kiện hoá nếu runner thiếu compose
   (ghi rõ, KHÔNG silent-skip).
Acceptance: contract assert bắt 1 mutation giả lập (đổi field → e2e đỏ, demo+revert) · CI
e2e job xanh trên main · gate đỏ thật khi e2e fail.

## §3 — T03: Durable LLM trace + cost spine (thi hành ADR-054 §2)

Nhận: W-93 · W-40 · W-55 · W-54 · W-74.
1. Migration V015 `llm_traces`: id, tenant_id, intent_type, rid, node, provider,
   model, tokens_in/out (từ usage_metadata), cost_usd (bảng giá env/config),
   latency_ms, status, error_code, created_at. Partition RANGE monthly NGAY TỪ
   ĐẦU (bài học W-66) + housekeeper nhận thêm bảng này (rolling +3 tháng).
   KHÔNG lưu raw prompt/response trong V015 (PII + dung lượng — ADR-041);
   thêm cột `payload_ref` NULL — chỗ cho object-store ref khi C4-media xong.
   Eval T04 dùng fixture riêng, không cần raw từ trace.
2. W-40: `llm_client.py` ghi trace mọi call (cả fallback) — qua MCP tool mới
   `traces.append` (ADR-003: AI không chạm DB trực tiếp) — fire-and-forget,
   lỗi ghi trace KHÔNG được làm fail request (try/except + counter).
3. W-55: OTel metrics — counter `llm.cost.usd` + `llm.tokens` label
   {tenant_id, intent, model, provider} + histogram latency.
4. W-54: fix trace propagation AI→MCP (inject/extract W3C traceparent ở MCP
   client + server) — 1 trace_id xuyên Gateway→AI→MCP, verify live bằng 1
   intent call trong Tempo/log.
5. W-74: Prometheus retention/rollup config (giảm full-granularity vô hạn).
Acceptance: 1 voice + 1 text intent live → rows llm_traces đúng tenant +
tokens>0 + cost>0 · cross-tenant assert (tenant A không SELECT được trace B —
RLS/app-scope) · trace_id xuyên 3 service nhìn thấy · kill MCP giữa call →
request vẫn hoàn thành (trace lỗi được nuốt + counter tăng) · housekeeper
tạo partition llm_traces (chạy 1 vòng live).

## §4 — T04: Eval harness + golden set v0 + prompt regression (thi hành ADR-054 phụ thuộc)

Nhận: W-32 · W-37.
1. Harness `apps/ai/eval/`: runner pytest-able chạy graph với LLM MOCK mode
   (fixture đáp án) mặc định + LIVE mode sau env flag (tốn tiền — chỉ tay).
   Metric per-case: intent đúng / tool-call đúng / output schema valid /
   (search) top-k chứa expected.
2. Golden set v0: 6 graph × ≥5 case = ≥30 case YAML/JSON fixture (tiếng Việt
   thật, lấy từ acceptance pages /dev/acceptance hiện có làm hạt giống).
3. W-37: prompt snapshot test — mọi file `apps/ai/src/prompts/` có snapshot;
   đổi prompt = snapshot diff = test fail → buộc update có chủ ý + chạy eval.
4. Wire CI (mock mode) — eval fail = pipeline fail. LIVE mode = script tay,
   ghi kết quả vào `apps/ai/eval/RESULTS.md` (baseline đầu tiên chạy 1 lần,
   số ghi sổ).
Acceptance: mock eval xanh CI · sửa 1 prompt → snapshot đỏ (demo + revert) ·
LIVE baseline chạy 1 lần, RESULTS.md có số thật ≥30 case · luật ADR-051 #2
("cấm refactor prompt khi chưa có eval") giờ ENFORCEABLE — ghi 1 dòng vào
CLAUDE.md DoD trỏ harness.

## §5 — T05: k6 baseline + SLO đo được + ops polish

Nhận: W-77 · W-56 · W-78 · W-79 · A13(phần còn).
1. W-77: k6 scripts (login · POST /intent text · GET cart · stream-start) —
   chạy local compose, ghi baseline vào `docs/PERF_BASELINE.md`: p50/p95/p99
   + error-rate @ 50/100/200 VU, đối chiếu NFR (500 RPS sustained là target
   hạ tầng prod — baseline compose chỉ là MỐC, ghi rõ không phải verdict).
2. W-56: SLO khắc thành recording rules Prometheus (p95 REST · p95
   stream-start · error-rate · availability proxy) — đo được từ hôm nay;
   alert rules đầy đủ = P0 #7/C8 (chỉ định nghĩa + record ở đây).
3. W-78 v0: failure-injection tối thiểu — env flag ép primary LLM fail →
   verify fallback OpenAI hoạt động (test nhúng); ghi nhận pattern cho CB
   C3-RT.
4. W-79: template rollback-note bắt buộc trong migration header (V015 đã
   theo) + check CI: migration mới thiếu "-- ROLLBACK:" → fail.
Acceptance: PERF_BASELINE.md có số thật + so NFR · recording rules query được
trên Prometheus live · kill-switch LLM primary → fallback pass test · CI bắt
migration thiếu rollback-note (demo + revert).

## §6 Exit criteria slice

5 task ✅ · CI từ soft → HARD toàn tuyến (đỏ là đỏ thật) · eval harness chạy
trong CI · llm_traces tích data thật (≥1 ngày trước khi C7 mở) · 17 ID tick
trong map §3 (W-78 v0 tick 🟡 nếu chỉ minimal — ghi residual→C3-RT) · nghi
thức §8 + OUTPUT R4 + Pha D.

> Sau slice này: ADR-054 có data spine (cost đ/intent-call bắt đầu đo — NFR §1
> chốt số sau 2 tuần) · C3/C3-RT/C7 được PHÉP mở vì lưới đã căng.
