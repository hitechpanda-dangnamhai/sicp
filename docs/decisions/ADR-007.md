# ADR-007 — Single Flask app for all 8 intent subgraphs
- **Status:** Accepted
- **Context:** Có thể split mỗi intent thành microservice riêng.
- **Decision:** 1 Flask app, 8 subgraphs trong cùng process. LangGraph router dispatch.
- **Rationale:** Hackathon scope, deployment đơn giản, share state easier.

## Amendment 2026-06-13 (S-P0-02/T01) — giữ 1 app, runtime đổi sang production server
Giữ quyết định **1 Flask app / 8 subgraph** (KHÔNG đổi). Runtime **dev server**
(`flask run` — `apps/ai/Dockerfile:71`) thay bằng **production server** (gunicorn/
uvicorn workers) tại **C3**. ADR-007 chưa bao giờ quyết định dev-server — amend làm
rõ để KHÔNG ai cite nó biện hộ cho `flask run` ở production (finding A1 INVENTORY).
